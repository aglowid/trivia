<%= form_for(game_question) do |f| %>
  <% if game_question.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(game_question.errors.count, "error") %> prohibited this game_question from being saved:</h2>

      <ul>
      <% game_question.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :game_id %>
    <%= f.number_field :game_id %>
  </div>

  <div class="field">
    <%= f.label :question_id %>
    <%= f.number_field :question_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
