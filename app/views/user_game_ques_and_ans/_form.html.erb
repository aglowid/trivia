<%= form_for(user_game_ques_and_an) do |f| %>
  <% if user_game_ques_and_an.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user_game_ques_and_an.errors.count, "error") %> prohibited this user_game_ques_and_an from being saved:</h2>

      <ul>
      <% user_game_ques_and_an.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %>
    <%= f.number_field :user_id %>
  </div>

  <div class="field">
    <%= f.label :game_id %>
    <%= f.number_field :game_id %>
  </div>

  <div class="field">
    <%= f.label :question_id %>
    <%= f.number_field :question_id %>
  </div>

  <div class="field">
    <%= f.label :answer %>
    <%= f.text_field :answer %>
  </div>

  <div class="field">
    <%= f.label :is_correct_ans %>
    <%= f.check_box :is_correct_ans %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
